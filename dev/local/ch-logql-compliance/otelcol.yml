receivers:
  filelog:
    include: [/var/flogs/*.json]
exporters:
  otlp/oteldb:
    endpoint: oteldb:4317
    tls:
      insecure: true
service:
  pipelines:
    logs:
      receivers: [filelog]
      exporters: [otlp/oteldb]
