{
    "abort_on_alert"=%true;
    "compression_thread_count"=4;
    writers={
        stderr={
            type=stderr;
            format=json;
        };
    };
    rules=[
        {
            "min_level"=debug;
            writers=[
                stderr;
            ];
            family=structured;
        };
    ];
}