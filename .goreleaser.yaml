version: 2
project_name: oteldb
builds:
  - id: oteldb
    binary: "oteldb{{ .Ext }}"
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
      - riscv64
    main: ./cmd/oteldb
    mod_timestamp: "{{ .CommitTimestamp }}"
    flags:
      - -trimpath
  - id: docker-logql
    binary: "docker-logql{{ .Ext }}"
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
      - riscv64
    main: ./cmd/docker-logql
    mod_timestamp: "{{ .CommitTimestamp }}"
    flags:
      - -trimpath

archives:
  - name_template: >-
      {{ .ProjectName }}-
      {{- .Os }}-
      {{- .Arch  }}
    format_overrides:
      - goos: windows
        formats: ["zip"]
    builds_info:
      group: root
      owner: root
    files:
      - README.md
      - LICENSE

release:
  # prerelease: allow # remove this when we start publishing non-prerelease or set to auto
  # draft: true # allow for manual edits
  github:
    owner: go-faster
    name: oteldb

  mode: replace
  draft: false
  prerelease: auto

  # Templates: allowed
  footer: |
    ## Docker images

    ```
    {{ .Env.IMAGE }}:v{{ .Version }}
    ```

dockers_v2:
  - id: distroless
    images:
      - "ghcr.io/oteldb/oteldb"

    dockerfile: release.Dockerfile

    platforms:
      - linux/amd64
      - linux/arm64

    tags:
      - "v{{ .Version }}"
      - "{{ if .IsNightly }}nightly{{ end }}"
      - "{{ if not .IsNightly }}latest{{ end }}"

    build_args:
      IMG: "gcr.io/distroless/static"

    labels:
      "io.artifacthub.package.readme-url": "https://github.com/oteldb/oteldb/blob/main/README.md"
      "io.artifacthub.package.logo-url": "https://avatars.githubusercontent.com/u/131437363"
      "io.artifacthub.package.maintainers": '[{"name":"Aleksandr Razumov","email":"ernado@go-faster.org"}]'
      "io.artifacthub.package.license": "Apache 2.0"

      "org.opencontainers.image.description": "OpenTelemetry signal storage"
      "org.opencontainers.image.created": "{{.Date}}"
      "org.opencontainers.image.name": "{{.ProjectName}}"
      "org.opencontainers.image.revision": "{{.FullCommit}}"
      "org.opencontainers.image.version": "{{.Version}}"
      "org.opencontainers.image.source": "{{.GitURL}}"

  - id: clickhouse
    images:
      - "ghcr.io/oteldb/oteldb"

    dockerfile: release.Dockerfile

    platforms:
      - linux/amd64

    tags:
      - "v{{ .Version }}-clickhouse"
      - "{{ if .IsNightly }}nightly-clickhouse{{ end }}"
      - "{{ if not .IsNightly }}latest-clickhouse{{ end }}"

    build_args:
      IMG: "clickhouse/clickhouse-server"

    labels:
      "io.artifacthub.package.readme-url": "https://github.com/oteldb/oteldb/blob/main/README.md"
      "io.artifacthub.package.logo-url": "https://avatars.githubusercontent.com/u/131437363"
      "io.artifacthub.package.maintainers": '[{"name":"Aleksandr Razumov","email":"ernado@go-faster.org"}]'
      "io.artifacthub.package.license": "Apache 2.0"

      "org.opencontainers.image.description": "OpenTelemetry signal storage"
      "org.opencontainers.image.created": "{{.Date}}"
      "org.opencontainers.image.name": "{{.ProjectName}}"
      "org.opencontainers.image.revision": "{{.FullCommit}}"
      "org.opencontainers.image.version": "{{.Version}}"
      "org.opencontainers.image.source": "{{.GitURL}}"

checksum:
  name_template: "checksums.txt"
snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^test:"
      - "^chore"
      - "merge conflict"
      - Merge pull request
      - Merge remote-tracking branch
      - Merge branch
      - go mod tidy
  groups:
    - title: "Helm chart updates"
      order: 700
      regexp: '^.*?(feat|fix|chore)\(helm\)!?:.+$'
    - title: "Dependency updates"
      regexp: '^.*?(feat|fix|chore)\(deps\)!?:.+$'
      order: 300
    - title: "New Features"
      regexp: '^.*?feat(\(\S+\))??!?:.+$'
      order: 100
    - title: "Security updates"
      regexp: '^.*?sec(\(\S+\))??!?:.+$'
      order: 150
    - title: "Performance updates"
      regexp: '^.*?perf(\(\S+\))??!?:.+$'
      order: 250
    - title: "Bug fixes"
      regexp: '^.*?fix(\(\S+\))??!?:.+$'
      order: 200
    - title: "Documentation updates"
      regexp: '^.*?docs(\(\S+\))??!?:.+$'
      order: 400
    - title: "Benchmarking tooling changes"
      regexp: '^.*?(\(\S*bench\S*\))!?:.+$'
      order: 450
    - title: "Build process updates"
      regexp: '^.*?build(\(\S+\))??!?:.+$'
      order: 500
    - title: "CI/CD updates"
      regexp: '^.*?ci(\(\S+\))??!?:.+$'
      order: 600
    - title: "Updates to tests"
      regexp: '^.*?test(\(\S+\))??!?:.+$'
      order: 600

    - title: Other work
      order: 9999

sboms:
  - artifacts: archive

nfpms:
  - id: oteldb
    package_name: oteldb
    ids:
      - oteldb
    homepage: https://github.com/go-faster/oteldb
    maintainer: Aleksandr Razumov <ernado@go-faster.org>
    description: OpenTelemetry signal storage
    license: Apache 2.0
    formats:
      - apk
      - deb
      - rpm
      - archlinux
    deb:
      lintian_overrides:
        - statically-linked-binary
        - changelog-file-missing-in-native-package
  - id: docker-logql
    package_name: docker-logql
    ids:
      - docker-logql
    homepage: https://github.com/go-faster/oteldb/tree/main/cmd/docker-logql
    maintainer: Aleksandr Razumov <ernado@go-faster.org>
    description: A simple Docker CLI plugin to run LogQL queries over container logs.
    license: Apache 2.0
    # Install to /usr/lib/docker/cli-plugins, as it would be expected by Docker CLI.
    bindir: /usr/lib/docker/cli-plugins
    formats:
      - apk
      - deb
      - rpm
      - archlinux
    deb:
      lintian_overrides:
        - statically-linked-binary
        - changelog-file-missing-in-native-package
